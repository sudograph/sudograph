type File {
    id: ID!
    created_at: Date!
    name: String!
    file_type: FileType!
    chunks: [FileChunk!]!
}

type FileChunk {
    id: ID!
    start_byte: Int!
    end_byte: Int!
    bytes: Blob!
}

enum FileType {
    TXT
    PDF
    PNG
    JPEG
}